apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    {{- include "sc.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sc.selectorLabels" (dict "component" "web") | nindent 4 }}
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
