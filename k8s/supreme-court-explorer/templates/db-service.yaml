apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    {{- include "sc.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sc.selectorLabels" (dict "component" "db") | nindent 4 }}
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
